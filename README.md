# softsove

Softsove website (Let's Get Weird, Let's Get Hired, Behind the Weird).

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` in `.env.local` (default `http://127.0.0.1:8000`) so careers and apply talk to the FastAPI backend.
